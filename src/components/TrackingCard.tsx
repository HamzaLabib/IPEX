import { FormEvent, useState } from 'react'; import { mockShipment } from '../data/mockShipments';
export default function TrackingCard(){const [q,setQ]=useState('');const [show,setShow]=useState(false);const [error,setError]=useState('');const submit=(e:FormEvent)=>{e.preventDefault();if(!q.trim()){setError('Please enter a tracking number to continue.');setShow(false);return;}setError('');setShow(true)};return <section className='container tracking-card-section'><div className='card card--form' style={{padding:'1.5rem'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'flex-start',gap:'1rem',flexWrap:'wrap'}}><div><h3 style={{color:'var(--purple)',margin:'0 0 .35rem'}}>Track Your Shipment</h3><p className='field-note'>Enter your tracking number for a quick shipment status update without leaving the homepage.</p></div></div><form onSubmit={submit} className='grid' style={{gridTemplateColumns:'1fr auto',gap:'1rem',marginTop:'1rem'}}><input className='input' placeholder='Enter Tracking Number...' value={q} onChange={e=>setQ(e.target.value)}/><button className='btn btn-green'>Track</button></form>{error&&<p className='field-error'>{error}</p>}{show&&<div style={{marginTop:'1rem'}}><p><b>Status:</b> {mockShipment.status}</p><p><b>Location:</b> {mockShipment.currentLocation}</p><p><b>ETA:</b> {mockShipment.estimatedDelivery}</p><p className='muted'>{mockShipment.timeline.join(' • ')}</p></div>}<div className='grid grid-4' style={{marginTop:'1.25rem'}}>{[['Shipments in Transit','24'],['Shipments Delivered','33'],['Pending Shipments','11'],['Payments Due','CAD 1,020.00']].map(([k,v])=><div className='card' style={{padding:'.8rem'}} key={k}><small>{k}</small><h3 style={{margin:0,color:'var(--purple)'}}>{v}</h3></div>)}</div></div></section>}
