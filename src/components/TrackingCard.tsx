import { FormEvent, useState } from 'react'; import { mockShipment } from '../data/mockShipments';
export default function TrackingCard(){const [q,setQ]=useState('');const [show,setShow]=useState(false);const submit=(e:FormEvent)=>{e.preventDefault();if(q.trim())setShow(true)};return <section className='container' style={{marginTop:'-2.5rem'}}><div className='card' style={{padding:'1.2rem'}}><h3 style={{color:'var(--purple)'}}>Track Your Shipment</h3><form onSubmit={submit} className='grid' style={{gridTemplateColumns:'1fr auto'}}><input className='input' placeholder='Enter Tracking Number...' value={q} onChange={e=>setQ(e.target.value)}/><button className='btn btn-green'>Track</button></form>{show&&<div><p><b>Status:</b> {mockShipment.status}</p><p><b>Location:</b> {mockShipment.currentLocation}</p><p><b>ETA:</b> {mockShipment.estimatedDelivery}</p><p className='muted'>{mockShipment.timeline.join(' • ')}</p></div>}<div className='grid grid-4'>{[['Shipments in Transit','24'],['Shipments Delivered','33'],['Pending Shipments','11'],['Payments Due','CAD 1,020.00']].map(([k,v])=><div className='card' style={{padding:'.8rem'}} key={k}><small>{k}</small><h3 style={{margin:0,color:'var(--purple)'}}>{v}</h3></div>)}</div></div></section>}
