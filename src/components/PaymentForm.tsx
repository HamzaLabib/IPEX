import { FormEvent, useState } from 'react';
const api=import.meta.env.VITE_API_BASE_URL || 'http://localhost:5000/api';
export default function PaymentForm(){const [msg,setMsg]=useState('');const onSubmit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const fd=new FormData(e.currentTarget);const body=Object.fromEntries(fd.entries());const amount=Number(body.amount);if(!body.invoiceNumber||!body.customerName||!String(body.email).includes('@')||amount<=0||!body.paymentMethod){setMsg('Please complete all required fields with valid values.');return;}try{const res=await fetch(`${api}/payments/create-checkout-session`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await res.json();if(data.checkoutUrl)window.location.href=data.checkoutUrl;else setMsg(`Development mode: ${data.reference||'IPEX-PAY-2025-0001'} submitted.`);}catch{setMsg('Payment request submitted in mock mode. Ref: IPEX-PAY-2025-0001');}};return <form className='card' style={{padding:'1rem'}} onSubmit={onSubmit}><div className='form-grid'>{[['invoiceNumber','Invoice Number'],['customerName','Customer Name'],['email','Email Address'],['phone','Phone Number'],['amount','Payment Amount CAD']].map(([n,l])=><div key={n}><label>{l}</label><input name={n} required className='input' type={n==='email'?'email':n==='amount'?'number':'text'} min={n==='amount'?0.01:undefined}/></div>)}</div><label>Payment Method</label><select name='paymentMethod' required><option value=''>Select method</option><option>Credit/Debit Card</option><option>Interac e-Transfer</option><option>Bank Transfer</option></select><label>Notes / Reference Message</label><textarea name='notes' className='input'/><button className='btn btn-green' style={{marginTop:'1rem'}}>Proceed to Payment</button>{msg&&<p>{msg}</p>}</form>}
